APOLLO_API_KEY=
APIFY_TOKEN=
# Visit business websites for emails during Google Maps scrapes (extra Apify credits)
APIFY_SCRAPE_CONTACTS=true
PORT=5000
# Optional: where the file-backed lead store lives (default: ./data)
LEADS_DATA_DIR=
